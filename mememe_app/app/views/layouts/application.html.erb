<!DOCTYPE html>
<html>
<head>
  <title>MememeApp</title>
  <link href='https://fonts.googleapis.com/css?family=Roboto+Condensed' rel='stylesheet' type='text/css'>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <nav class="navbar navbar-default navbar-fixed-top" role="navigation">
    <div class="container-fluid">
      <div class="navbar-header">
        <a class="navbar-brand" href="/">
          <img id="logo" src="<%= asset_path('mememe-logo.png') %>" alt="mememe">
        </a>
      </div>
      <ul id="nav-links" class="nav navbar-nav">
        <% if current_user %>
        <li id="nav-welcome" class="navbar-brand"><%= current_user.username %> logged in</li>
        <li class="navbar-brand"><%= link_to 'logout', logout_path, method: :delete %></li>
        <% else %>
        <li class="navbar-brand"><%= link_to 'LOGIN', new_session_path %></li>
        <li class="navbar-brand"><%= link_to 'SIGN UP', new_user_path %></li>
        <% end %>
      </ul>
    </div>
  </nav>

<div id="content">

<%= yield %>

</div>

</body>
</html>
